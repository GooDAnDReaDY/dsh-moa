# @goodandready/dsh-moa

> Плагин **Mixture of Agents (MoA)** для **DeepSeek Harness**, добавляющий слэш-команду `/moa <prompt>`.

---

## Обзор

`dsh-moa` реализует архитектуру **Mixture of Agents** в DeepSeek Harness по образцу решений в Hermes Agent и OpenClaw.

Вместо отправки сложной инженерной или логической задачи одной модели, `dsh-moa` выполняет двухфазный ансамблевый процесс:
1. **Модели-кандидаты (Proposers / Advisors):** Несколько независимых моделей параллельно анализируют задачу и формируют независимые решения.
2. **Модель-агрегатор (Judge / Synthesizer):** Мощная рассуждающая модель сопоставляет ответы кандидатов, устраняет ошибки, отбирает лучшие идеи и синтезирует финальный выверенный ответ.
3. **One-Shot восстановление модели:** Команда `/moa` действует строго на один ход. Сразу после получения ответа сессия автоматически и гарантированно (в блоке `finally`) возвращается к исходной модели сессии.

---

## Возможности

- **Интеграция слэш-команды (`/moa`):** Автодополнение в поле ввода чата DeepSeek Harness через сервис `inputTriggers`.
- **Параллельный опрос (Fan-out):** Конкурентное выполнение запросов с graceful degradation (при сбое одного кандидата процесс продолжается с оставшимися).
- **Изолированный контекст советников:** Очистка истории от системных промптов агента и вызовов инструментов для экономии токенов и исключения ложных отказов.
- **Нативная карточка настроек DSH:** Карточка в разделе «Настройки → Плагины → Mixture of Agents», использующая системные токены темы `--dsw-alias-*`.
- **Именованные пресеты:** Поддержка нескольких конфигураций моделей (`default`, `code-review`, `deep-reasoning`).

---

## Использование

В поле ввода чата DeepSeek Harness:

```text
/moa напиши архитектуру распределённого кэша с защитой от dog-piling
```

Или с указанием конкретного пресета:

```text
/moa deep-reasoning найди скрытые гонки данных в этом алгоритме
```

---

## Настройки

В разделе **Настройки → Плагины → Mixture of Agents**:

| Параметр | Описание | По умолчанию |
|---|---|---|
| **Основной пресет** | Имя пресета по умолчанию при вызове `/moa <prompt>` | `default` |
| **Модели-кандидаты** | Массив `{ provider, model }`, опрашиваемых параллельно | `deepseek:deepseek-chat`, `openai:gpt-4o` |
| **Ведущая модель (судья)** | Модель-агрегатор, синтезирующая финальный ответ | `anthropic:claude-3-7-sonnet` |
| **Температура кандидатов** | Степень креативности кандидатов | `0.6` |
| **Температура судьи** | Степень точности синтезатора | `0.4` |
| **Максимум токенов** | Лимит длины ответа | `4096` |

---

## Тестирование

Запуск модульных и контрактных тестов без обращения к внешней сети:

```bash
node --test test/*.test.mjs
```

---

## Лицензия

MIT © [GooDAnDReaDY](https://github.com/GooDAnDReaDY)
